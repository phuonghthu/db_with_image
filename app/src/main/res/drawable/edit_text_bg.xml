<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="6dp" /> <!-- Bán kính bo góc -->
    <stroke
        android:width="1dp"
        android:color="@color/darkblue" /> <!-- Màu viền -->
</shape>